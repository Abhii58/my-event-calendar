// src/shadcn.js
import { Button } from 'shadcn';

// Example usage of a Button component from shadcn
const ShadcnButton = () => {
    return (
        <Button>
            Click Me
        </Button>
    );
};

export default ShadcnButton;
